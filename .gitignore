# Block everything — only .enc files and README may be committed
*

# Allow repo infrastructure
!.gitignore
!Makefile

# The only data files allowed are encrypted Verilog
!*.enc

# Allow README files (mywork/README.md)
!README.md
!mywork/
!mywork/README.md
